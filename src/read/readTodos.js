const readTodos = (projects) => {
  const rootDiv = document.createElement('div');
  rootDiv.classList.add('projects');
  rootDiv.textContent = 'All projects!!!';

  return rootDiv;
};

export default readTodos;
